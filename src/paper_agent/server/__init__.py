"""Web API 服务层（FastAPI）：CLI 之外的第二入口。

设计约定（M7）：
- 业务编排全部复用 services/ 与 graph/，路由只做参数解析、状态码映射和 JSON 序列化；
- 长任务（流水线）走 jobs 后台线程 + 轮询；轻操作（检索/问答/对话）同步返回；
- 每个请求/任务独立打开自己的 Library 连接（SQLite 文件库，无共享连接）。
"""
